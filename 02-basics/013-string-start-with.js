/**
 * check whether a string starts with spacific character/s and false otherwise.
 */

function str_start_with(str, start_with) {
  return str.startsWith(start_with);
}

console.log(str_start_with('moataz samir', 'mo'));
